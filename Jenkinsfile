#!/usr/bin/env groovy

pipeline {
  agent {
    dockerfile {
       additionalBuildArgs '--name jkdfmd'
    }
  }
  stages {
    stage('build') {
      steps {
      echo "where am I?"
      }
    }
}
